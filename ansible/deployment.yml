---
- hosts: localhost
  gather_facts: no
  connection: local
  vars_files: 
   - vars.yml
  vars:
    LABEL: "app={{ APPLICATION_NAME }}"
  tasks:
    - name: Object queries
      include_tasks: tasks/get_object.yml

    - name: Deploy RC app
      include_tasks: tasks/create_object.yml
